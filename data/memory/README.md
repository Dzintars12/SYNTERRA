# SYNTERRA Persistent Memory Data

This directory is reserved for local development memory files.

Runtime memory records may be written as JSONL files during local development.

Production deployments should use a database or managed storage provider instead of committing runtime memory files to Git.
